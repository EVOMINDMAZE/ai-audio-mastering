# =============================================================================
# AI Audio Mastering & Analysis Tool — Python dependencies
# Phase 1 (backend + audio_engine.py DSP pipeline)
# =============================================================================

# ---- Web framework -----------------------------------------------------------
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.12

# ---- Audio analysis ----------------------------------------------------------
librosa==0.10.2
soundfile==0.12.1
numpy==1.26.4
scipy==1.13.1

# ---- Audio I/O / conversion --------------------------------------------------
pydub==0.25.1
ffmpeg-python==0.2.0

# ---- DSP chain ---------------------------------------------------------------
# Spotify pedalboard provides production-grade HPF, EQ, Compressor, Limiter, Gain.
pedalboard==0.9.9
# pyloudnorm implements ITU-R BS.1770-4 — the spec Spotify/Apple/YouTube use.
pyloudnorm==0.1.1

# ---- Supabase (Phase 2 / storage wiring) -------------------------------------
supabase==2.8.1

# ---- Config ------------------------------------------------------------------
pydantic==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ---- Dev / test --------------------------------------------------------------
pytest==8.3.3
httpx==0.27.2